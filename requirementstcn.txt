# TCN Pretraining Requirements
# Minimal dependencies for standalone TCN pretraining on SPY RV

# Core ML
torch>=2.0.0
numpy>=1.24.0

# Data handling
pandas>=2.0.0
pyarrow>=12.0.0  # For parquet files

# Logging (optional, uses built-in logging if not available)
tqdm>=4.65.0
